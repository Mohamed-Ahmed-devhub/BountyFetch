// notificationJob.js — stub (Bull removed; Redis not required)
// TODO: implement push/email notifications when needed
function initNotificationJob() {
  console.log('ℹ️  Notification job: stub mode (no Bull/Redis required)')
}
module.exports = { initNotificationJob }
